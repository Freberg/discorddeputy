resources:
  - namespace.yaml
  - rabbitmq
  - mongo
  - api
  - bot-interactive
  - bot-notifier
  - processor
  - fetcher-epic
#  - fetcher-steam

patches:
  - path: actuator-probes-patch.yaml
    target:
      kind: Deployment
      #labelSelector: "app in (fetcher-epic, api, processor, bot-notifier)"
      labelSelector: "app in (fetcher-epic, bot-notifier)"